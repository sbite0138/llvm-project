# The mini-rv32ima harness (mtg_rv32.c) is an end-to-end integration
# test that needs an external simulator (ursa, separate repo) to run —
# it doesn't fit lit's FileCheck workflow. Exclude it from discovery
# until we wire up an %ursa substitution; see the file's header comment
# for the manual-invocation recipe and the TODO.
config.excludes = ["mtg_rv32.c"]
